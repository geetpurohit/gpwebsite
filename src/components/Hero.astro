---
const chips = ['Machine Learning', 'XAI Frameworks', 'Causal Inference', 'Python & SQL', 'Georgia Tech MS'];
const stats = [
  { num: '$1.2B+', label: 'Bad-debt impact' },
  { num: '73%', label: 'Faster hypotheses' },
  { num: '3+ yrs', label: 'At Verizon' },
  { num: '2×', label: 'STEM degrees' },
];
---

<header id="hero">
  <div class="hero-inner container">
    <p class="tag"><span class="ping"></span>Data Scientist at Verizon&nbsp;·&nbsp;Open to opportunities</p>

    <h1 class="title">
      <span class="static-lines">
        <span class="line">Astrophysicist.</span>
        <span class="line grad-text">Data Scientist.</span>
        <span class="line">Storyteller.</span>
      </span>
      <span class="type-line" aria-hidden="true" hidden>
        <span class="typed grad-text"></span><span class="caret"></span>
      </span>
    </h1>

    <p class="sub">
      I used to study black holes. Now I find them in datasets instead.
      <em>Both are equally mysterious, and both need a concerning amount of coffee.</em>
    </p>

    <div class="chips">
      {chips.map((c) => <span class="chip">{c}</span>)}
    </div>

    <div class="ctas">
      <a href="#projects" class="btn btn-primary">See my work&nbsp;↓</a>
      <a href="#contact" class="btn btn-ghost">Get in touch</a>
    </div>

    <dl class="stats">
      {stats.map((s) => (
        <div class="stat">
          <dt class="stat-num">{s.num}</dt>
          <dd class="stat-label">{s.label}</dd>
        </div>
      ))}
    </dl>
  </div>

  <a href="#about" class="scroll-hint" aria-label="Scroll to content">
    <span>Scroll</span>
    <span class="scroll-line"></span>
  </a>
</header>

<style>
  #hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: clamp(110px, 16vh, 170px) var(--pad-x) clamp(60px, 9vh, 90px);
    overflow: hidden;
    z-index: 2;
  }
  .hero-inner { position: relative; z-index: 2; max-width: 760px; }

  .tag {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    font-family: var(--font-display);
    font-size: clamp(10px, 1.3vw, 12px);
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--purple-lt);
    margin-bottom: 26px;
    opacity: 0;
    animation: fadeUp 0.8s var(--ease) forwards 0.15s;
  }
  .ping {
    width: 8px; height: 8px; border-radius: 50%;
    background: var(--purple);
    box-shadow: 0 0 0 0 rgba(168,85,247,0.6);
    animation: ping 2.2s var(--ease) infinite;
  }
  @keyframes ping {
    0%   { box-shadow: 0 0 0 0 rgba(168,85,247,0.6); }
    70%  { box-shadow: 0 0 0 10px rgba(168,85,247,0); }
    100% { box-shadow: 0 0 0 0 rgba(168,85,247,0); }
  }

  .title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(44px, 8.5vw, 104px);
    line-height: 0.98;
    letter-spacing: -0.04em;
    margin-bottom: 24px;
  }
  .title .line { display: block; opacity: 0; animation: fadeUp 0.85s var(--ease) forwards; }
  .static-lines .line:nth-child(1) { animation-delay: 0.28s; }
  .static-lines .line:nth-child(2) { animation-delay: 0.40s; }
  .static-lines .line:nth-child(3) { animation-delay: 0.52s; }

  .type-line {
    display: block;
    min-height: 1.05em;
    opacity: 0;
    animation: fadeUp 0.85s var(--ease) forwards 0.28s;
  }
  .type-line[hidden] { display: none; }
  .caret {
    display: inline-block;
    width: 0.06em;
    margin-left: 0.04em;
    background: var(--purple-lt);
    transform: translateY(0.12em);
    align-self: stretch;
    animation: caretBlink 1s steps(1) infinite;
  }
  .typed:empty + .caret,
  .typed + .caret { height: 0.92em; }
  @keyframes caretBlink {
    0%, 49% { opacity: 1; }
    50%, 100% { opacity: 0; }
  }

  .sub {
    font-size: clamp(17px, 1.5vw, 21px);
    color: var(--text-dim);
    font-weight: 300;
    line-height: 1.7;
    max-width: 500px;
    margin-bottom: 30px;
    opacity: 0;
    animation: fadeUp 0.85s var(--ease) forwards 0.66s;
  }
  .sub em { color: var(--muted); font-style: italic; }

  .chips {
    display: flex; flex-wrap: wrap; gap: 9px;
    margin-bottom: 34px;
    opacity: 0;
    animation: fadeUp 0.85s var(--ease) forwards 0.78s;
  }
  .chip {
    font-size: 13px;
    padding: 7px 15px;
    border-radius: 999px;
    border: 1px solid rgba(168,85,247,0.26);
    background: rgba(168,85,247,0.06);
    color: var(--lilac);
    backdrop-filter: blur(4px);
  }

  .ctas {
    display: flex; flex-wrap: wrap; gap: 13px;
    margin-bottom: 52px;
    opacity: 0;
    animation: fadeUp 0.85s var(--ease) forwards 0.9s;
  }
  .btn {
    position: relative;
    padding: 15px 32px;
    border-radius: 10px;
    font-size: 14.5px;
    font-weight: 500;
    letter-spacing: 0.01em;
    transition: transform 0.25s var(--ease), box-shadow 0.25s, opacity 0.25s, background 0.25s, border-color 0.25s, color 0.25s;
    overflow: hidden;
  }
  .btn-primary {
    background: linear-gradient(120deg, var(--violet), var(--purple));
    color: #fff;
    box-shadow: 0 8px 30px -10px rgba(168,85,247,0.7);
  }
  .btn-primary::after {
    content: '';
    position: absolute; inset: 0; left: -120%;
    width: 60%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.28), transparent);
    transform: skewX(-20deg);
    transition: left 0.6s var(--ease);
  }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 12px 36px -8px rgba(168,85,247,0.85); }
  .btn-primary:hover::after { left: 140%; }
  .btn-ghost {
    border: 1px solid var(--border);
    color: var(--text-dim);
    background: rgba(255,255,255,0.02);
  }
  .btn-ghost:hover { color: var(--text); border-color: rgba(168,85,247,0.5); background: rgba(168,85,247,0.06); }

  .stats {
    display: flex;
    flex-wrap: wrap;
    gap: clamp(22px, 4vw, 48px);
    opacity: 0;
    animation: fadeUp 0.85s var(--ease) forwards 1.02s;
  }
  .stat { display: flex; flex-direction: column; gap: 2px; }
  .stat-num {
    font-family: var(--font-display);
    font-size: clamp(22px, 2.6vw, 30px);
    font-weight: 700;
    letter-spacing: -0.02em;
    color: var(--text);
  }
  .stat-label {
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--muted);
  }

  .scroll-hint {
    position: absolute;
    bottom: 30px; left: 50%;
    transform: translateX(-50%);
    display: flex; flex-direction: column; align-items: center; gap: 8px;
    opacity: 0;
    animation: fadeUp 0.9s var(--ease) forwards 1.3s;
  }
  .scroll-hint span:first-child {
    font-family: var(--font-display);
    font-size: 10px; letter-spacing: 0.3em; text-transform: uppercase;
    color: var(--muted);
  }
  .scroll-line {
    width: 1px; height: 42px;
    background: linear-gradient(var(--purple), transparent);
    animation: scrollPulse 2s var(--ease) infinite;
  }
  @keyframes scrollPulse {
    0%, 100% { opacity: 0.35; transform: scaleY(0.7); transform-origin: top; }
    50%      { opacity: 1;    transform: scaleY(1); }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(22px); }
    to   { opacity: 1; transform: none; }
  }

  @media (max-width: 820px) {
    .scroll-hint { display: none; }
  }
</style>

<script>
  const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const staticLines = document.querySelector('.static-lines') as HTMLElement | null;
  const typeLine = document.querySelector('.type-line') as HTMLElement | null;
  const typedEl = document.querySelector('.type-line .typed') as HTMLElement | null;

  if (!reduce && staticLines && typeLine && typedEl) {
    staticLines.hidden = true;
    typeLine.hidden = false;

    const words = ['Astrophysicist.', 'Data Scientist.', 'Storyteller.'];
    let wi = 0, ci = 0, deleting = false;

    const tick = () => {
      const word = words[wi];
      ci += deleting ? -1 : 1;
      typedEl.textContent = word.slice(0, ci);

      if (!deleting && ci === word.length) {
        deleting = true;
        setTimeout(tick, 1500);
      } else if (deleting && ci === 0) {
        deleting = false;
        wi = (wi + 1) % words.length;
        setTimeout(tick, 450);
      } else {
        setTimeout(tick, deleting ? 45 : 85 + Math.random() * 55);
      }
    };

    setTimeout(tick, 1100);
  }
</script>
